version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ananahnu_db
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=ananahnu
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - ananahnu_net

volumes:
  postgres_data:


networks:
  ananahnu_net:
